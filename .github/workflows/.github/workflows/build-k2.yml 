name: Build K2 16MB

on:
  workflow_dispatch: # 允许手动点击

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Show Info
        run: |
          echo "开始准备编译环境..."
          echo "这里需要填入具体的编译脚本"
          # 注意：如果你没有完整的编译脚本，这里只会跑一个空壳
          # 但只要你保存了这个文件，左侧列表就会出现 "Build K2 16MB"
